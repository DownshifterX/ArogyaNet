# ========================================
# ArogyaNet Frontend Environment Variables
# ========================================

# Backend API Configuration
# The URL of your ArogyaNet backend server
VITE_BACKEND_URL="http://localhost:3000"

# Socket.IO Server Configuration (optional)
# If not set, defaults to VITE_BACKEND_URL
VITE_SOCKET_URL="http://localhost:3000"

# WebRTC TURN Server Configuration (optional)
# TURN API URL for fetching ICE server credentials
# Leave commented out if using default STUN servers only
# VITE_TURN_API_URL="https://your-turn-server.com/api/credentials"

# WebRTC ICE Configuration (optional)
# Set to 'true' to force relay-only connections (no direct P2P)
# VITE_ICE_RELAY_ONLY="false"

# STUN servers for WebRTC (comma-separated list)
# If not specified, defaults to Google and Twilio public STUN servers
# VITE_STUN_SERVERS="stun:stun.l.google.com:19302,stun:global.stun.twilio.com:3478,stun:stun1.l.google.com:19302,stun:stun2.l.google.com:19302,stun:stun3.l.google.com:19302,stun:stun4.l.google.com:19302"

# ========================================
# Encryption Configuration
# ========================================

# Encryption Salt
# Used for deriving encryption keys from user IDs
# IMPORTANT: Change this to a unique, random value for your application
# Keep this value secret and consistent across deployments
# If you change this value, previously encrypted documents will be unreadable
VITE_ENCRYPTION_SALT="arogyanet_secure_medical_platform_2025"

# ChaCha20-Poly1305 encryption is implemented client-side
# Keys are automatically derived from user accounts
# No manual key management needed
